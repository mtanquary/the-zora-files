{
  "name": "title-export",
  "version": "1.0.0",
  "private": true,
  "description": "Renders the Finding Zora title card reveal to transparent PNG frames for video compositing.",
  "scripts": {
    "render": "node render.js"
  },
  "dependencies": {
    "puppeteer": "^23.0.0"
  }
}
